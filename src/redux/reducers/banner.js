let initialState = [
    "./images/Banner1.jpg",
    "./images/Banner2.jpg",
    "./images/Banner3.jpg",
  ];
  
  const banners = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default banners;